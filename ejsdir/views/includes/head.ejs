<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= instaData.username %>'s Instagram</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            text-align: center;
            padding: 30px;
        }
        .profile {
            background-color: white;
            border-radius: 20px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            display: inline-block;
            padding: 20px 40px;
        }
        .profile img {
            border-radius: 50%;
            width: 120px;
            height: 120px;
        }
        .name {
            font-size: 28px;
            color: #333;
            margin-top: 10px;
        }
        .bio {
            color: #666;
            font-style: italic;
        }
        .stats {
            display: flex;
            justify-content: center;
            margin-top: 20px;
            gap: 20px;
        }
        .stat {
            background-color: #ffebcd;
            border-radius: 10px;
            padding: 10px 20px;
        }
        .posts {
            margin-top: 40px;
        }
        .post {
            display: inline-block;
            margin: 10px;
        }
        .post img {
            border-radius: 10px;
            width: 200px;
            height: 200px;
            object-fit: cover;
        }
        .caption {
            color: #444;
            font-size: 16px;
            margin-top: 5px;
        }
    </style>
</head>